from setuptools import setup, find_packages

setup(
    name="mushroom_classification",
    version="0.1.0",
    packages=find_packages(), # Bu 'src' papkasini va uning ichidagilarni avtomatik topadi
)